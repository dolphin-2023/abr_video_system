# Project Notes

这个目录只放工程说明、论文写作辅助材料和本地资料索引。PDF、DOCX、原始数据集和训练产物默认被 `.gitignore` 排除，避免把大文件或第三方论文误提交到 GitHub。

推荐保留：

```text
docs/data_policy.md   数据集与可清理产物边界
docs/testing.md       测试、编译检查和训练前检查命令
```

本地论文资料可以放在 `docs/references/` 和 `docs/thesis/`，但这两个目录不会进入 Git。
